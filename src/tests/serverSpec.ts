describe('Basic Test', () => {
    it('should make jasmine-ts just work', () => {
        expect(5).toBe(5)
    })

    it('should make jasmine-ts just work again', () => {
        expect(5).toBe(5)
    })

    it('should make jasmine-ts just work again again', () => {
        expect(5).toBe(5)
    })
})
